KUBERNETES FEATURE STATUS: experimental
Policy: docs/KubernetesSupport.md (schema version
{{ index .Chart.Annotations "oxibelt.dev/kubernetes-support-policy" }})

Rendering or installing this chart is qualification evidence only; it does not
by itself establish supported Kubernetes behavior. The Gateway-controller
pairing requires Kubernetes 1.34, 1.35, or 1.36 and all mandatory graduation
gates. Unsupported combinations are reported by Helm, the controller
`/supportz` endpoint, and `oxibeltctl doctor --kubernetes`.

{{- if and .Values.admin.enabled .Values.admin.service.enabled (has .Values.admin.service.type (list "LoadBalancer" "NodePort")) (not .Values.admin.mtls.enabled) (eq .Values.admin.mtls.enforcement "optional") }}
WARNING: the Admin Service is externally exposed without mTLS. TLS 1.3 and the
bearer token remain required, but every client that can obtain the token can
reach the Admin API. Set `admin.mtls.enabled=true` or use a stricter
`admin.mtls.enforcement` policy for production workloads.
{{- end }}
