# PROXY protocol TLS regressions

This directory is reserved for minimized, reviewed PROXY v2 SSL-TLV parser
regressions. Add each reproducer to `tests/rust/fuzz_regressions.rs` with a
deterministic replay before merging.
