[package]
name = "pq-probe"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
anyhow = "1.0.102"
rustls = { version = "0.23.39", default-features = false, features = ["aws_lc_rs", "logging", "std", "tls12"] }
