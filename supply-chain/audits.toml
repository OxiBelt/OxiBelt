
# cargo-vet audits file

[[audits.aes]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.9.1 -> 0.9.2"
notes = "Reviewed the complete checksum-matched 0.9.1-to-0.9.2 source delta and the rewritten hardware backends. Runtime cpufeatures tokens still select the active union variant before every x86 or AArch64 target-feature call, SIMD loads and stores traverse exact typed-array extents with compile-time divisibility checks, the portable fixslice backend is safe Rust, and the crate has no build script or external side effects."

[[audits.aes]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.9.2 -> 0.9.3"
notes = "Reviewed the complete checksum-matched 0.9.2-to-0.9.3 source delta. VAES-256 and VAES-512 dispatch is gated by runtime cpufeatures tokens before target-feature intrinsics, typed-array batch loads and stores retain fixed extents, and the portable backend remains safe Rust. AArch64 AES is disabled under Miri, removed cfg flags only selected the now-default VAES backends, and no build script or external capability was added."

[[audits.aes-gcm]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.11.0 -> 0.11.1"
notes = "Reviewed the complete checksum-matched 0.11.0-to-0.11.1 source delta; AES-GCM tag validation replaces subtle with ctutils full-slice constant-time equality while preserving reject-before-decrypt behavior. The new zeroize marker is feature-gated and OxiBelt enables only aes and alloc. No unsafe code, build script, FFI, I/O, networking, or command execution; feature-matched KAT/Wycheproof and OxiBelt provider round-trip tests pass."

[[audits.aho-corasick]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.1.4 -> 1.1.5"
notes = "Reviewed the complete checksum-matched 1.1.4-to-1.1.5 source delta; Span and Match offsetting now use checked addition instead of release-mode wrapping, packed-search range preconditions are documented, and the existing SIMD search unsafe blocks are unchanged. The crate adds no build script or external side effects."

[[audits.android_system_properties]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.1.5 -> 0.1.6"
notes = "Reviewed the complete checksum-matched 0.1.5-to-0.1.6 source delta. The Android property callback now uses the correct extern C ABI and void return, carries an Option<String> cookie, documents its pointer invariants, and maps invalid UTF-8 to None instead of unwinding through FFI. The fallback buffer still consumes only the length initialized by __system_property_get; no build script or new external capability was added."

[[audits.anyhow]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.104"
notes = "Reviewed complete 1.0.104 source and the 1.0.103 delta; error-erasure, downcast, pointer-ownership, and formatting-buffer unsafe invariants are contained, the build script only performs a Cargo-rustc probe in OUT_DIR, and the source delta is documentation-only."

[[audits.argon2]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.6.0"
notes = "Reviewed the complete checksum-matched 0.6.0 source and the 0.5.3 delta. The fallible aligned block allocator validates Layout, handles null, exposes only its exact zero-initialized extent, and deallocates with the same immutable layout. The private segment-view unsafe layer restricts writes to one lane and slice and cross-lane reads away from the active slice; validated parameter invariants prevent zero lanes and segments, and sequential plus Rayon KAT and PHC suites pass. Legacy v16 and v19 PHC verification and mismatch rejection are preserved, allocation failure maps fail closed, MSRV 1.85 is below the workspace floor, and the crate has no build script or external I/O."

[[audits.async-compression]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.4.42 -> 0.4.43"
notes = "Reviewed the complete checksum-matched 0.4.42-to-0.4.43 source delta; the decoder now repolls only when the current input buffer was fully consumed, preserving progress and caller-buffer bounds, while all codec implementations and unsafe boundaries are unchanged and the crate performs no new filesystem, network, process, or build-time activity."

[[audits.aws-lc-fips-sys]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.13.15 -> 0.13.16"
notes = "Reviewed the complete checksum-matched 0.13.15-to-0.13.16 source delta and verified its embedded upstream commit signature. The FIPS native module is unchanged; generated bindings retain their layouts and signatures apart from the versioned symbol prefix and a layout-preserving va_list alias, while build changes add a bounded header-derived module-version constant and share the reviewed compiler-probe and system-library validation changes from aws-lc-sys."

[[audits.aws-lc-fips-sys]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.13.16 -> 0.14.1"
notes = "Reviewed the complete checksum-matched cached 0.13.16-to-0.14.1 delta and its locally available provenance. The crate moves from AWS-LC-FIPS 3.4.0/API 30 to the FIPS 4.x branch, 4.1.0/API 35, with regenerated bindings and a broad native module update including ML-DSA, ML-KEM, entropy, self-check, and X.509 surfaces. Its build remains a local CMake/compiler/bindgen boundary with no downloader or network client; the added FIPS small-build warning correctly identifies a configuration that can affect compliance. OxiBelt does not select aws-lc-rs/fips, so this update does not alter its selected runtime crypto module."

[[audits.aws-lc-rs]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.17.1 -> 1.17.3"
notes = "Reviewed the complete checksum-matched 1.17.1-to-1.17.3 source delta and verified its embedded upstream commit signature. The facade adds version reporting over a static AWS-LC string, clarifies ML-DSA seed and expanded-key serialization, and otherwise delegates to separately audited sys deltas; the new CStr conversion uses an initialized library-owned NUL-terminated constant and does not alter OxiBelt's selected algorithms or features."

[[audits.aws-lc-rs]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.17.3 -> 1.18.0"
notes = "Reviewed the complete checksum-matched cached 1.17.3-to-1.18.0 delta and its locally available provenance. The facade adds checked out-of-place TLS AEAD sealing, fixes key-wrap short-input rejection, zeroizes key-wrap keys, and stabilizes ML-DSA-facing APIs while retaining deprecated compatibility aliases. OxiBelt's first-party mutation-pqc features use the stable signature API and no longer request aws-lc-rs/unstable; the locked quic-parser 0.1.5 dependency still requests that feature independently for its AWS-LC backend. The new AEAD FFI call validates all caller-controlled output lengths before invocation; OxiBelt continues to use only the existing in-place AEAD methods and its ML-DSA-44 signature lengths remain explicitly checked."

[[audits.aws-lc-sys]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.42.0 -> 0.43.0"
notes = "Reviewed the complete checksum-matched 0.42.0-to-0.43.0 source delta and verified its embedded upstream commit signature. Generated FFI remains layout-compatible, Windows prebuilt NASM disassembly and relocations are unchanged after symbol-prefix normalization, and the native delta adds CTS APIs, null-checks optional AEAD cleanup, honors explicitly configured unexpected-EOF handling, and adds opt-in shared-library symbol versioning. Build-script changes remain confined to toolchain probes, caller-selected system-library inputs, and OUT_DIR; OxiBelt retains vendored static AWS-LC with the existing bindgen-only RISC-V feature."

[[audits.aws-lc-sys]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.43.0 -> 0.44.0"
notes = "Reviewed the complete checksum-matched cached 0.43.0-to-0.44.0 delta and its locally available provenance. The embedded AWS-LC revision advances to 991e67ff4cf04df4dd89e407f8b920c6936cb56a; generated bindings preserve examined ABI layouts while advancing AWS-LC 5.2.0 to 5.5.0 and adding EVP_AEAD_CTX_copy. Native changes cover maintained cipher, bignum, CPU-dispatch, entropy, ML-DSA, and ML-KEM sources. Build changes remain local to selected compiler/CMake/bindgen tooling and OUT_DIR, add writable copies for read-only stores and link metadata, and introduce no download or network behavior. OxiBelt's riscv64-musl bindgen feature remains local libclang generation."

[[audits.base64]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.22.1 -> 0.23.0"
notes = "Reviewed the complete 0.22.1-to-0.23.0 source delta, including custom-padding and decode-error changes plus the new AVX2 and NEON engines; SIMD loads and stores are guarded by slice bounds, instruction dispatch is runtime-checked or requires explicit unsafe construction, and the scalar implementation remains free of unsafe code. OxiBelt's direct declarations disable default features and select std, while the existing clubcard-crlite edge enables simd-unsafe through Cargo feature unification."

[[audits.base64]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.23.0 -> 0.23.1"
notes = "Reviewed the complete checksum-matched 0.23.0-to-0.23.1 crate delta; production encoding, decoding, SIMD dispatch, and unsafe blocks are unchanged, while tests now compile the generic SIMD wrapper only on supported architectures. OxiBelt's direct declarations continue to disable default features and select std, the existing clubcard-crlite edge continues to enable simd-unsafe through Cargo feature unification, and the crate has no build script or external side effects."

[[audits.blake2]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.10.6 -> 0.11.0"
notes = "Reviewed the complete checksum-matched 0.10.6-to-0.11.0 source delta. The digest 0.11 rewrite removes optional SIMD intrinsics and retains one unsafe byte-slice conversion over a fully initialized repr(C) tuple of four homogeneous integers, whose exact layout contains no padding. Fixed and variable output bounds and keyed initialization are checked, known-answer tests pass, and the crate has no build script or external side effects. OxiBelt selects no blake2 features."

[[audits.cc]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.4.0"
notes = "Reviewed complete 1.4.0 source and the 1.2.67 delta; compiler discovery and invocation remain explicit build-time behavior confined to caller-selected inputs and OUT_DIR, and the functional delta only propagates Cargo trim-path settings into compiler prefix-map flags that OxiBelt does not set."

[[audits.cc]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.4.0 -> 1.4.2"
notes = "Reviewed the complete checksum-matched 1.4.0-to-1.4.2 source delta. Compiler invocation remains build-time and caller-controlled; changes correct leaf-frame-pointer flags, ARM NEON selection, target spelling and platform tables, preserve fallible flag probing, and update the separately reviewed MSVC locator. Temporary files and subprocesses remain confined to the existing build boundary and OUT_DIR."

[[audits.clang-sys]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.8.1 -> 1.9.1"
notes = "Reviewed the complete checksum-matched 1.8.1-to-1.9.1 source delta, including Clang 19/20 and rewriter FFI declarations. New libclang entry points remain unsafe extern calls or dynamically checked symbols with C-compatible declarations; build discovery only extends the existing caller-controlled library search to LIBRARY_PATH, Cygwin, and corrected Visual Studio paths, and static linking explicitly asks llvm-config for static libraries."

[[audits.clap]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "4.6.4"
notes = "Reviewed complete 4.6.4 source and the 4.6.2 delta; the facade forbids unsafe code, only reexports clap_builder and optional derive support, has no build-time side effects, and the source delta is documentation-only."

[[audits.clap]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.6.4 -> 4.6.5"
notes = "Reviewed the complete checksum-matched 4.6.4-to-4.6.5 source delta; the facade still forbids unsafe code and has no build script or external IO, production source changes only documentation, while package metadata advances the separately audited clap_builder dependency and adds example snapshot coverage."

[[audits.clap]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.6.5 -> 4.6.6"
notes = "Reviewed the complete checksum-matched 4.6.5-to-4.6.6 source delta. The safe-Rust facade remains production-identical apart from advancing the separately audited clap_builder dependency; other changes add example-only bounded alias expansion and documentation. The crate has no build script or external side effects."

[[audits.clap_builder]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.6.2 -> 4.6.5"
notes = "Reviewed the complete checksum-matched 4.6.2-to-4.6.5 source delta; it corrects help-display bracketing for partially optional option values and documents existing empty-range assertions without changing argument parsing, and the crate continues to forbid unsafe code and has no build script or external side effects."

[[audits.clap_builder]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.6.5 -> 4.6.6"
notes = "Reviewed the complete checksum-matched 4.6.5-to-4.6.6 source delta. Argument conflict bookkeeping is renamed consistently, missing-value validation and usage rendering are corrected, and the overridden-usage getter exposes existing state. Parsing remains safe Rust and input-bounded, with no build script or filesystem, network, or process capability."

[[audits.clap_derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "4.6.4"
notes = "Reviewed complete 4.6.4 proc-macro source and the 4.6.1 delta; it forbids unsafe code and has no file, network, or process effects, while the delta normalizes literal expression parsing and updates AST matching for Syn 3."

[[audits.clubcard]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.3 -> 0.3.4"
notes = "Reviewed the complete checksum-matched 0.3.3-to-0.3.4 source delta; inverted approximate ribbons encode the complement consistently, exact-filter insertions are pruned against the approximate filter's pre-inversion equations, and exact ribbons inherit and apply the block inversion. Builders reject items on the wrong side of the encoded set; the only unsafe code remains an unchanged test-only slice reinterpretation, and no build script or external side effect was added."

[[audits.clubcard-crlite]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.6.0 -> 0.6.1"
notes = "Reviewed the complete checksum-matched 0.6.0-to-0.6.1 source delta; V4 serialization now rejects lengths, counts, ranks, and offsets that exceed their wire prefixes instead of truncating them, while decoding and membership-query behavior remain unchanged. The crate contains no unsafe code or build script, and its only dependency-line change advances base64 from 0.22 to the separately audited 0.23 line."

[[audits.clubcard-crlite]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.6.1 -> 0.6.2"
notes = "Reviewed the complete checksum-matched 0.6.1-to-0.6.2 source delta; V4 serialization now rejects block identifiers that are not the exact 32-byte issuer SPKI hash consumed by the decoder, preventing stream desynchronization. Other production changes expose the existing inversion flag in diagnostics, while complement-filter and oversized-offset behavior gain tests; the crate contains no unsafe code or build script."

[[audits.combine]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.6.7 -> 4.6.8"
notes = "Reviewed the complete checksum-matched 4.6.7-to-4.6.8 production-source delta. BufReader replaces BytesMut chunk access with spare-capacity access, limits each read to 8 KiB, initializes the complete exposed MaybeUninit slice before casting it to bytes, verifies the reader-reported length, and advances only the initialized prefix. Other runtime changes are lifetime, trait, Hash-derive, and re-export maintenance; no dependency, build-script, filesystem, network, or process capability is added."

[[audits.compio-runtime]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.12.3 -> 0.12.4"
notes = "Reviewed the complete checksum-matched 0.12.3-to-0.12.4 source delta; poll-backed AsyncWrite now delegates vectored writes and flushes to the underlying descriptor and half-closes connected sockets on close. The Unix shutdown call receives a live BorrowedFd raw descriptor, Windows distinguishes files from sockets, non-socket and unconnected errors remain idempotent success, existing unsafe operation and ownership invariants are unchanged, and no build script was added."

[[audits.compio-runtime]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.12.4 -> 0.12.5"
notes = "Reviewed the complete checksum-matched 0.12.4-to-0.12.5 source delta; the only production change repairs Windows WSA event-interest bookkeeping by atomically masking cleared events and re-arming the remaining active mask. Socket and handle ownership, unsafe calls, non-Windows behavior, dependencies, and build-time behavior are unchanged, and the crate has no build script."

[[audits.cpufeatures]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.0 -> 0.3.1"
notes = "Reviewed the complete checksum-matched 0.3.0-to-0.3.1 source delta. X86 AVX detection now requires the correct YMM operating-system state, Miri uses compile-time target-feature detection, and deprecated maximum-value calls are replaced without semantic change. Existing architecture-gated CPUID and XGETBV unsafe intrinsics retain their caller invariants, no unsafe block is added, and the crate has no build script or new external capability."

[[audits.crc32fast]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.5.0 -> 1.5.1"
notes = "Reviewed the complete checksum-matched 1.5.0-to-1.5.1 package delta. The new x86 AVX2 and AVX-512 and AArch64 interleaved CRC paths gate target-feature calls through CPU detection, keep unaligned loads within checked slice extents, and add differential coverage across thresholds, alignments, initial states, and streaming splits. The baseline rewrite is little-endian equivalent, the build script still only queries the configured rustc version to gate stabilized intrinsics, and no production dependency or external capability was added."

[[audits.ctor]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.10"
notes = "Audited full source of ctor 1.0.10; startup hooks require explicit unsafe acknowledgement, linker registration delegates to typed link-section boundaries, and Static publishes initialized storage through acquire and release state transitions with poisoning; the new dereference fast path reads only after observing INITIALIZED."

[[audits.ctor]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.0.10 -> 1.0.12"
notes = "Reviewed the complete checksum-matched 1.0.10-to-1.0.12 source delta; the only production change pointer-aligns the Darwin force-link .quad anchor with p2align 3, strengthening rather than weakening the linker-record layout invariant, with startup synchronization, unsafe contracts, and external side effects otherwise unchanged."

[[audits.ctor]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.0.12 -> 1.0.13"
notes = "Reviewed the complete checksum-matched 1.0.12-to-1.0.13 source delta; production code only adds generated re-export documentation and advances the separately audited link-section and linktime-proc-macro patch releases. Constructor registration, startup synchronization, unsafe contracts, and external side effects are unchanged, and no build script is present."

[[audits.data-encoding]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.11.0 -> 2.11.1"
notes = "Reviewed the complete checksum-matched 2.11.0-to-2.11.1 crate delta; production source, table-generation logic, unsafe blocks, and dependencies are byte-for-byte unchanged, with only package metadata and lockfile maintenance. The crate has no build script or external side effects."

[[audits.deadpool]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.13.0 -> 0.13.1"
notes = "Reviewed the complete checksum-matched 0.13.0-to-0.13.1 source delta. The selected Tokio managed-pool implementation remains safe Rust and its public pool behavior is unchanged; the release removes num_cpus and replaces its default-size helper with available_parallelism while the itertools update is development-only. No build script or external capability was added."

[[audits.displaydoc]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.6 -> 0.2.7"
notes = "Reviewed the complete checksum-matched 0.2.6-to-0.2.7 proc-macro delta; it migrates equivalent generated Display bounds and AST construction to Syn 3, raises MSRV to 1.71, contains no unsafe code or build script, and performs no filesystem, environment, network, or process operations."

[[audits.either]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.17.0"
notes = "Reviewed complete 1.17.0 source and the 1.16.0 delta; the existing Pin projection unsafe blocks and invariants are unchanged, the functional delta only forwards additional formatting traits to the selected variant, and the crate retains Rust 1.63, the same dual license, and no build script."

[[audits.encoding_rs_io]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.1.7 -> 0.1.8"
notes = "Reviewed the complete checksum-matched 0.1.7-to-0.1.8 source delta; BOM replay now tracks only the bounded three-byte prefix instead of accumulating the total stream length, removing a long-stream overflow without changing stripping or decoding semantics. The crate contains no unsafe code or build script and performs no new filesystem, network, or process activity."

[[audits.enum-ordinalize]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.4.1 -> 4.4.2"
notes = "Reviewed the complete checksum-matched 4.4.1-to-4.4.2 source delta; production code, generated ordinal conversions, unsafe APIs, and dependencies are unchanged, and the release only adds documentation showing checked Serde deserialization through from_ordinal."

[[audits.enum-ordinalize-derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.4.1 -> 4.4.2"
notes = "Reviewed the complete checksum-matched 4.4.1-to-4.4.2 proc-macro delta; production macro source and generated ordinal and unsafe-transmute code are byte-for-byte unchanged, while dependencies move to the already admitted Syn 3, quote, and proc-macro2 versions. The crate has no build script, filesystem, network, process, or environment access."

[[audits.event-listener]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "5.4.1 -> 5.4.2"
notes = "Reviewed the complete checksum-matched 5.4.1-to-5.4.2 source delta. The selected std path retains mutex-guarded intrusive-list access; the new no-std fallback spinlock gives exclusive UnsafeCell access through acquire/release atomics and a drop guard, and StackSlot Send/Sync now correctly requires T: Send. No build script or external IO was added."

[[audits.fancy-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.18.0 -> 0.19.0"
notes = "Reviewed the complete checksum-matched 0.18.0-to-0.19.0 source delta selected through jsonschema. New byte-input and RegexSet APIs, HIR compilation, and opt-in seek paths remain safe Rust. The VM retains the default 1,000,000-backtrack limit and 1,000,000-branch stack ceiling; pooled scratch can retain peak capture, backtrack, and delegated-engine allocations. RegexSet has documented 64-MiB meta-NFA and DFA-cache defaults but is not used by jsonschema, while OxiBelt's production validator explicitly selects the linear-time regex engine. Case-folded backreferences now require full equality, and invalid quantifiers and misplaced G anchors fail or fall back safely. The MIT-licensed crate has no build script or external IO."

[[audits.fastrand]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.5.0"
notes = "Reviewed complete 2.5.0 source and the 2.4.1 delta; the crate forbids unsafe code, implements a documented non-cryptographic RNG with bounded range sampling, is not used for OxiBelt cryptographic randomness, and the source delta is documentation-only."

[[audits.find-msvc-tools]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.1.9 -> 0.1.10"
notes = "Reviewed the complete checksum-matched 0.1.9-to-0.1.10 source delta. Windows-only tool discovery fixes Visual Studio instance enumeration, executable selection, and WinAPI declarations while retaining initialized COM ownership and registry/path validation. It adds no runtime dependency, build script, network access, or caller-independent process execution."

[[audits.fraction]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.16.0 -> 0.17.0"
notes = "Reviewed the complete checksum-matched 0.16.0-to-0.17.0 crate delta and its signed upstream release commit. Numeric-component parsers now reject embedded signs, validate radices before dispatch, report zero denominators explicitly for GenericFraction radix parsing, and use checked multiplication and addition for Unicode mixed numerators instead of wrapping or panicking; work and BigInt allocation remain proportional to input. OxiBelt selects only with-bigint. Optional Decimal, DynaInt, Unicode, and Juniper changes follow the same fail-closed parsing contract; existing unsafe fieldless-error Send/Sync implementations and the guarded generic cast are unchanged, and the crate has no build script or external side effect."

[[audits.futures]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed the complete 0.3.33 facade and 0.3.32 delta together with every selected Futures companion crate; it adds no build script or external side effects and keeps the same MSRV and license while exposing the reviewed soundness and leak fixes."

[[audits.futures]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 facade delta; production source is byte-for-byte unchanged apart from synchronized dependency versions, with no unsafe, build script, or external capability added."

[[audits.futures-channel]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 channel source and the 0.3.32 delta; channel ownership, wakeup, cancellation, and close paths remain bounded and contain no build script or external side effects."

[[audits.futures-channel]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 channel delta; production source is byte-for-byte unchanged, preserving ownership, wakeup, cancellation, and close behavior without a build script or external side effect."

[[audits.futures-core]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 core source and the 0.3.32 delta; the crate remains a side-effect-free trait layer with no build script, and pinning contracts are unchanged."

[[audits.futures-core]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 core delta; production source is byte-for-byte unchanged and remains a side-effect-free trait layer with no build script."

[[audits.futures-executor]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 executor source and the 0.3.32 delta; local and thread-pool scheduling preserve task ownership and wakeup invariants, with no build script or new external side effects."

[[audits.futures-executor]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 executor delta; production source is byte-for-byte unchanged, preserving task ownership and wakeup behavior with no build script or new capability."

[[audits.futures-io]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 I/O trait source and the 0.3.32 delta; the crate remains a side-effect-free interface layer with no unsafe implementation, build script, or external operation."

[[audits.futures-io]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 I/O trait delta; production source is byte-for-byte unchanged and remains a side-effect-free interface layer without unsafe code or a build script."

[[audits.futures-macro]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 proc-macro source and the 0.3.32 delta; generated select and join state machines retain pinning and ownership checks and the macro performs no filesystem, network, or process effects."

[[audits.futures-macro]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 proc-macro delta; expansion source is byte-for-byte unchanged and only the separately audited Syn 3 parser line advances. The macro performs no filesystem, network, process, or build-script activity."

[[audits.futures-sink]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 sink source and the 0.3.32 delta; the crate remains a side-effect-free trait layer with unchanged pinning and readiness contracts."

[[audits.futures-sink]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 sink delta; production source is byte-for-byte unchanged and remains a side-effect-free trait layer with unchanged pinning and readiness contracts."

[[audits.futures-task]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 task source and the 0.3.32 delta; ArcWake, waker, and spawn ownership invariants remain contained and the crate has no build script or external side effects."

[[audits.futures-task]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 task delta; the only production change adds inlining to the pre-existing RawWaker clone helper, without changing its pointer, ownership, or reference-count invariants. No build script or external capability is added."

[[audits.futures-util]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.33"
notes = "Reviewed complete 0.3.33 utility source and the 0.3.32 delta; it fixes the IterPinRef Send contract, FuturesUnordered IntoIter leak, ReadLine exception safety, and compat01 Miri behavior while preserving pinning, initialization, and wakeup invariants. Optional spin and portable-atomic-util edges are not selected by OxiBelt."

[[audits.futures-util]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.33 -> 0.3.34"
notes = "Reviewed the checksum-matched 0.3.33-to-0.3.34 utility delta; the only production change adds inlining to a pre-existing RawWaker clone helper, while other edits skip Miri-only doctests. Pinning, initialization, wakeup, and reference-count invariants are unchanged, with no build script or external capability."

[[audits.glob]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.3.4"
notes = "Reviewed the complete 0.3.4 source and 0.3.3 diff; it contains no unsafe, build script, or write side effects, and the update only adds error conversion and caches filenames while preserving caller-controlled lazy filesystem traversal."

[[audits.granit-parser]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.0.7 -> 1.0.1"
notes = "Reviewed the complete checksum-matched production-source delta. The no_std parser continues to forbid unsafe code and has no build script or external capability. It adds structured terminal errors and fallible buffered input so I/O, decoding, and byte-limit failures cannot be mistaken for EOF; NUL and invalid controls reject instead of truncating, counters use checked arithmetic, and malformed-input states terminate. Resource budgets remain supplied by the consuming serde-saphyr layer."

[[audits.granit-parser]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.1.0 -> 1.2.0"
notes = "Reviewed the complete checksum-matched 1.1.0-to-1.2.0 production-source delta. The parser continues to forbid unsafe code and has no build script or external capability. New checked limits bound retained directive bytes, reserved directive parameters, and block-collection nesting; limit and malformed-input paths return structured errors instead of allowing unbounded memory or stack growth."

[[audits.h2]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.4.15 -> 0.4.16"
notes = "Reviewed the complete checksum-matched h2 0.4.15-to-0.4.16 production-source delta. The HTTP/2 receive path now charges a capped 25,600-byte framing-overhead budget for small and empty DATA frames, replenishing only when payload is delivered and issuing ENHANCE_YOUR_CALM on exhaustion, which bounds the advisory's queued-empty-frame resource attack. The remaining changes preserve protocol safety: 1xx push accounting is one-time, dropped buffered data releases capped flow-control capacity, trailers retain a waker, resets after received EOS preserve readable data, and zero-byte writes fail instead of spinning. Send buffering flushes outside stream locks; trailer headers receive existing RFC validation; HPACK reuses only fully copied scratch buffers and replaces its generated 4-bit decoder table with a bounds-checked 8-bit representation that rejects invalid EOS and padding. No dependency, feature, build-script, FFI, or unsafe-code change was introduced."

[[audits.h2]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.4.18 -> 0.4.19"
notes = "Reviewed the complete checksum-matched 0.4.18-to-0.4.19 production-source delta. The default DATA-frame budget now scales to half the configured connection window with a 25,600-frame floor, while empty DATA frames have an independent lifetime cap of 100 and cannot replenish that allowance; explicitly configured budgets retain their prior semantics. The update adds no dependency, feature, build-script, FFI, or unsafe-code change."

[[audits.http]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.4.2 -> 1.5.0"
notes = "Reviewed the complete checksum-matched 1.4.2-to-1.5.0 source delta. Header validation still scans every byte before the existing checked UTF-8 conversion, URI parsing replaces match ladders with equivalent byte-class tables and adds a MAX_LEN rejection before u16 offsets, empty builder paths are normalized without relaxing direct PathAndQuery parsing, and no changed unsafe, build, or IO boundary was found."

[[audits.http-body-util]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.1.4 -> 0.1.5"
notes = "Reviewed the complete checksum-matched 0.1.4-to-0.1.5 source delta; the only production edit changes a docs.rs-only documentation cfg, leaving Body polling and collection behavior byte-for-byte unchanged. The crate adds no unsafe, build script, or external capability."

[[audits.hybrid-array]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.4.13 -> 0.4.14"
notes = "Reviewed the complete checksum-matched 0.4.13-to-0.4.14 source delta; it only adds compile-time Streamlined NTRU Prime size aliases and their ArraySize implementations, with no runtime code, unsafe change, build script, or external side effect."

[[audits.hyper]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.11.0"
notes = "Reviewed the complete checksum-matched 1.11.0 source and the 1.10.1-to-1.11.0 delta; the changed HTTP/1 parser removes Content-Length and disables keep-alive when Transfer-Encoding is present, changed ReadBufCursor methods preserve initialized-length and capacity invariants, and the remaining unsafe edits are equivalent pointer casts or existing guarded header/index operations. OxiBelt adds a bounded pre-parser guard that retains its stricter rejection policy before service dispatch."

[[audits.hyper]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.11.0 -> 1.11.1"
notes = "Reviewed the complete checksum-matched 1.11.0-to-1.11.1 source delta. HTTP/1 head completion now accepts mixed LF and CRLF in the same cases as the parser, while OxiBelt's bounded pre-parser remains authoritative for stricter framing rejection. ReadBuf, header, body-length, client-dispatch, HTTP/2, and upgrade changes preserve initialized-length, index, ownership, and wakeup invariants; no build script or external capability was added."

[[audits.indexmap]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.14.0 -> 2.14.1"
notes = "Reviewed the complete checksum-matched 2.14.0-to-2.14.1 source delta. Equivalent comparisons are simplified only where the query and key types are identical, unified bounds assertions preserve panic-before-access behavior, and uniqueness and index checks precede construction of disjoint mutable references. Existing localized pointer casts are unchanged in contract, and the crate has no build script or external capability."

[[audits.jiff]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.2.34"
notes = "Reviewed complete 0.2.34 source and the 0.2.32 delta; TZif and POSIX parsing uses checked lengths and explicit caps, expected system-zone filesystem and platform-FFI access is contained, pointer-tagged TimeZone and formatting-buffer unsafe invariants are maintained, and extracted core code was audited separately."

[[audits.jiff]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.34 -> 0.2.35"
notes = "Reviewed the complete checksum-matched 0.2.34-to-0.2.35 source delta; fractional duration units now mark a span nonzero so negative sub-second values retain their sign, while parser bounds, timezone IO, platform FFI, and all unsafe implementations are unchanged."

[[audits.jiff-core]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.1.0"
notes = "Reviewed complete new 0.1.0 source; the no-std civil-time and TZif/POSIX parsers use checked bounds and explicit resource caps, have no external side effects, and the sole ArrayStr unsafe conversion preserves initialized-length and UTF-8 invariants."

[[audits.jiff-static]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.2.34"
notes = "Reviewed complete 0.2.34 proc-macro source and the 0.2.32 delta; compile-time file access is limited to the explicitly supplied TZif path, parsing is delegated to audited jiff-core, and generated unsafe copying is restricted to a statically constructed TZif time zone."

[[audits.jiff-static]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.34 -> 0.2.35"
notes = "Reviewed the complete checksum-matched 0.2.34-to-0.2.35 crate delta; production proc-macro source is byte-for-byte unchanged and only package version metadata moved, so caller-selected TZif reads and generated-code invariants remain as previously audited."

[[audits.js-sys]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.3.103 -> 0.3.104"
notes = "Reviewed the complete checksum-matched 0.3.103-to-0.3.104 source delta. Single- and multi-thread task runners install drop guards that break future, waker, closure, and Rc cycles on Rust panic or JavaScript exception without disturbing normal polling; new JsNullable upcasts are type-level. Existing JavaScript imports are unchanged and no build-time capability is added."

[[audits.jsonpath-rust]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.6"
notes = "Reviewed the checksum-matched 1.0.6 archive and 1.0.5 diff; the only source change adds QueryRef getters, with no unsafe, build script, or IO, while recursive descendant queries remain caller-bounded."

[[audits.jsonpath-rust]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.0.6 -> 1.0.7"
notes = "Reviewed the complete checksum-matched 1.0.6-to-1.0.7 source delta after excluding line-ending churn. RFC 9535 string escapes now decode with checked four-hex and surrogate-pair parsing, normalized paths escape JSON Pointer members, loops and allocations remain proportional to input, and the crate contains no unsafe code, build script, filesystem, network, or process access."

[[audits.jsonpath-rust]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.0.7 -> 1.0.8"
notes = "Reviewed the complete checksum-matched 1.0.7-to-1.0.8 crate delta; production parser and query source is byte-for-byte unchanged, while package metadata drops unused development dependencies and stale documentation. The crate contains no unsafe code or build script and adds no external side effects."

[[audits.jsonpath-rust]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.0.8 -> 1.0.10"
notes = "Reviewed the complete checksum-matched 1.0.8-to-1.0.10 production-source delta. Parsing now performs a quote- and escape-aware precheck that rejects untrusted JSONPath input exceeding 128 nested brackets or parentheses before recursive parsing, preventing stack exhaustion without counting delimiters inside strings. The crate remains free of unsafe code, build scripts, and filesystem, network, or process access."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.48.5"
notes = "Reviewed the complete 0.48.5 source and 0.48.1 diff: there is no unsafe or build script, OxiBelt disables HTTP/file retrieval and selects Rust regex, and canonical JSON caps recursion at 255 and initial preallocation at 1 MiB."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.48.5 -> 0.49.2"
notes = "Reviewed the complete checksum-matched 0.48.5-to-0.49.2 source delta, including canonical schema algebra and generic value adapters. The crate has no unsafe or build script; canonical serialization caps recursion at 255, scratch reuse at eight 16-KiB buffers, initial preallocation at 1 MiB, and plain-number expansion at 1 MiB of digits. OxiBelt keeps default features disabled, so bundled HTTP and file retrieval remain absent."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.2 -> 0.49.4"
notes = "Reviewed the complete checksum-matched 0.49.2-to-0.49.4 source delta and upstream commit range. Validator changes correct additionalItems compilation and exact integer-versus-fractional bound comparisons, while evaluation paths reduce allocation without changing instance ownership. Canonicalization adds memoized reference, negation, unevaluated-item/property, and emptiness algebra behind the existing 255-depth and 1-MiB serialization bounds plus an explicit bounded intersection cache; the crate has no unsafe code or build script, and OxiBelt keeps HTTP and file retrieval disabled."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.4 -> 0.49.8"
notes = "Reviewed the complete checksum-matched 0.49.4-to-0.49.8 production canonicalization delta and its expanded tests. Canonical schema algebra gains exact rational, property-map, raw-gate, reference-fold, negation, and satisfiability handling while retaining canonicalization-specific 255-depth serialization, 1-MiB integer-digit and initial-preallocation caps, scratch-pool and fold budgets, and the capped intersection cache. New canonicalization options accept a caller-supplied retriever and base URI but perform no implicit IO; OxiBelt does not use canonicalization or those hooks. OxiBelt disables default features, so bundled HTTP and file retrieval remain absent, and its production validator selects the linear-time regex engine. The MIT-licensed crate contains no unsafe code or build script."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.8 -> 0.49.9"
notes = "Reviewed the complete checksum-matched 0.49.8-to-0.49.9 source delta. Changes are confined to canonicalization, which OxiBelt does not call: a one-million-meet budget fails partial expansion back to a raw operand, tagged oneOf folding requires a common required discriminator with pairwise-disjoint finite values, and sub-schema emission retains reachable definitions. Default HTTP and file retrieval remain disabled and no unsafe or build script is added."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.50.0 -> 0.51.0"
notes = "Reviewed the complete checksum-matched 0.50.0-to-0.51.0 crate delta and signed upstream release commits. Canonicalization gains bounded candidate witnesses and correctness fixes; validation fails closed on required unknown vocabularies, enforces format-assertion dialects, fixes recursive unevaluatedItems and unevaluatedProperties compilation with initialized cells plus instance-identity cycle guards, and corrects duration, URI-template, and ECMA-regex formats. OxiBelt disables default features, uses the safe serde_json adapter, explicitly selects Draft 7 and the linear Rust pattern engine for native configuration, and auto-detects only its fixed Draft 2020-12 admission schema; it does not use canonicalization, custom vocabularies, or format regex. HTTP and file retrieval remain absent, and no unsafe code, build script, or external side effect was added."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.51.0 -> 0.52.0"
notes = "Reviewed the complete checksum-matched 0.51.0-to-0.52.0 crate delta and its signed upstream release commit. Validation error traversal now collects directly into a vector proportional to the emitted errors and recursive validation checks its cycle guard before memoized results, preventing cached answers from overriding an active self-reference. Email and hostname validation gains checked, label-bounded Punycode decoding and correctness fixes; IDN formats are feature-gated and OxiBelt's schemas do not use them. Canonical reference folding adds at most eight settle rounds under the existing shared work budgets. OxiBelt keeps default features disabled, selects Draft 7 plus the linear Rust pattern engine for native configuration, and does not use canonicalization or external retrieval; HTTP, file, async, IDNA, macro, Python, and Ruby capabilities are absent from its selected graph. The selected source contains no unsafe code or build script."

[[audits.jsonschema]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.52.0 -> 0.52.1"
notes = "Reviewed the complete checksum-matched 0.52.0-to-0.52.1 selected production-source delta. Validation errors lazily cache owned instance values with safe synchronization, and multipleOf uses checked exact decimal arithmetic with a conservative fallback instead of imprecise floating-point division. OxiBelt keeps default features disabled, selects Draft 7 plus the linear Rust pattern engine, and does not enable external retrieval, canonicalization, Python, or Ruby adapters. The selected source contains no unsafe code, build script, or external capability."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.48.5"
notes = "Reviewed the complete 0.48.5 source and confirmed it is production-identical to 0.48.1; it is a safe-Rust ECMA regex translator with fallible parsing and no build or runtime IO side effects."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.48.5 -> 0.49.2"
notes = "Reviewed the complete checksum-matched 0.48.5-to-0.49.2 crate delta; production source is byte-for-byte unchanged and only package version metadata moved, preserving the safe-Rust fallible translator and its lack of build-time or runtime IO."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.2 -> 0.49.4"
notes = "Reviewed the complete checksum-matched 0.49.2-to-0.49.4 crate delta; production ECMA-regex translation source is byte-for-byte unchanged and only synchronized package metadata moved. The crate remains safe Rust with no build script, filesystem, network, process, or other external side effects."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.4 -> 0.49.8"
notes = "Reviewed the complete checksum-matched 0.49.4-to-0.49.8 crate delta. Production ECMA-regex translation source is byte-for-byte unchanged and only synchronized package metadata moved; the crate remains safe Rust with no build script or external side effects."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.8 -> 0.49.9"
notes = "Reviewed the complete checksum-matched 0.49.8-to-0.49.9 crate delta; production ECMA-regex translation source is byte-for-byte unchanged and only synchronized package metadata moved. The crate remains safe Rust with no build script or external side effect."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.50.0 -> 0.51.0"
notes = "Reviewed the complete checksum-matched 0.50.0-to-0.51.0 crate delta and signed upstream release commits. A fallible pattern-witness helper is used only by canonicalization and caps repeated expansion at 64 and witness checks at 256 bytes, while remaining allocation is proportional to the input pattern. The new non-recursive ECMA-262 Unicode syntax checker validates format regex with input-owned group-name bookkeeping; OxiBelt uses neither canonicalization nor that format and its existing Draft 7 pattern translation path is unchanged. The crate remains safe Rust with no build script or external side effect."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.51.0 -> 0.52.0"
notes = "Reviewed the complete checksum-matched 0.51.0-to-0.52.0 crate delta and signed upstream release commit; production ECMA-262 translation source is byte-for-byte unchanged and only synchronized package metadata moved. The crate remains safe Rust with no build script or external side effect."

[[audits.jsonschema-regex]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.52.0 -> 0.52.1"
notes = "Reviewed the complete checksum-matched 0.52.0-to-0.52.1 crate delta; production ECMA-262 translation source is byte-for-byte unchanged and only synchronized package metadata moved. The crate remains safe Rust with no build script or external side effect."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.48.5"
notes = "Reviewed the complete new 0.48.5 crate with its enabled serde_json feature; it contains no unsafe, build script, or IO side effects, and equality and unique-items allocations are proportional to caller input."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.48.5 -> 0.49.2"
notes = "Reviewed the complete checksum-matched 0.48.5-to-0.49.2 source delta. The selected serde_json adapter remains safe Rust with stable live-node identities and input-proportional equality storage. Optional Python handles stay GIL/lifetime anchored, optional Ruby raw values are rooted across GC and cache reuse is keyed by identity, GC count, and size; both adapters cap recursive materialization at 255 and preserve first-error reporting. No build script, network, filesystem, or process effect was added."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.2 -> 0.49.4"
notes = "Reviewed the complete checksum-matched 0.49.2-to-0.49.4 source delta; compiled numeric bounds retain exact i64 and u64 representations instead of rounding integers beyond 2^53 through f64, and arbitrary-precision fractional comparisons now prefer exact BigInt conversion. Generic value traversal and optional Python and Ruby unsafe adapter boundaries are unchanged, and the crate has no build script or external side effects."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.4 -> 0.49.8"
notes = "Reviewed the complete checksum-matched 0.49.4-to-0.49.8 source delta. Under the optional arbitrary-precision feature, comparisons now use exact BigFraction ordering for saturated or rounded-equal integer limits, and multipleOf rejects nonzero literals that underflow to f64 zero, correcting large and tiny-number rounding cases. OxiBelt does not enable arbitrary-precision and selects only the safe serde_json adapter; optional Python and Ruby unsafe adapters are unchanged. The MIT-licensed crate has no build script or external side effect."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.8 -> 0.49.9"
notes = "Reviewed the complete checksum-matched 0.49.8-to-0.49.9 crate delta; production value-adapter source is byte-for-byte unchanged and only synchronized package metadata moved. OxiBelt continues to select the safe serde_json adapter, with no build script or external side effect."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.50.0 -> 0.51.0"
notes = "Reviewed the complete checksum-matched 0.50.0-to-0.51.0 crate delta and signed upstream release commits; production source is byte-for-byte unchanged and only synchronized package metadata moved. OxiBelt continues to select the safe serde_json adapter, with no build script or external side effect."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.51.0 -> 0.52.0"
notes = "Reviewed the complete checksum-matched 0.51.0-to-0.52.0 crate delta and signed upstream release commit; all production value-adapter source is byte-for-byte unchanged and only synchronized package and dependency metadata moved. OxiBelt selects the safe serde_json adapter, while optional Python and Ruby unsafe adapters remain unselected and unchanged; the crate has no build script or external side effect."

[[audits.jsonschema-value]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.52.0 -> 0.52.1"
notes = "Reviewed the complete checksum-matched 0.52.0-to-0.52.1 production-source delta. The selected serde_json adapter adds a safe OnceLock-backed lazy instance value and exact decimal multipleOf parsing with checked arithmetic. Optional Python and Ruby adapters, including their unsafe boundaries, are unchanged and unselected. The crate has no build script or external side effect."

[[audits.kio]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.5.4"
notes = "Reviewed the complete checksum-matched kio 0.5.4 source newly introduced by web-transport-quinn. The crate contains no unsafe code or build script and no filesystem, environment, process, or network operations. WaiterList retains weak waker references, reclaims dead entries during registration, drains before waking outside locks, and Park retires registrations when task wakers change; Fan defers inline wakeups until projected-state locks are released. Production uses standard synchronization, while loom and thread spawning are test-only and optional time and Tokio features are not selected."

[[audits.kio]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.5.5 -> 0.5.6"
notes = "Reviewed the complete checksum-matched 0.5.5-to-0.5.6 production-source delta. WaiterList reduces its inline weak-waker slots from 32 to 4 while preserving heap spillover, dead-entry reclamation, snapshot draining, and wake-after-lock-release behavior. The crate remains free of unsafe code, build scripts, and filesystem, network, or process access."

[[audits.kube]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.0.0 -> 4.2.0"
notes = "Reviewed the complete checksum-matched 4.0.0-to-4.2.0 facade delta; production source and reexports are unchanged, the sole source edit annotates a disabled cluster test, and dependency metadata advances to the separately reviewed Kube family. The kube-client 4.2.0 trust boundary remains a separate exact exemption rather than being certified here."

[[audits.kube-core]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.0.0 -> 4.2.0"
notes = "Reviewed the complete checksum-matched 4.0.0-to-4.2.0 source delta; it exposes an existing serde_json source error in patch-serialization messages and removes a redundant debug borrow, with no unsafe code, build script, external IO, parser-bound, or schema-generation behavior change."

[[audits.kube-runtime]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "4.0.0 -> 4.2.0"
notes = "Reviewed the complete checksum-matched 4.0.0-to-4.2.0 source delta. Predicate caching uses one HashMap entry lookup, treats TTL-stale entries as absent before amortized cleanup, and retains input-bounded cache state; owner triggering takes owned metadata instead of cloning it, and the reconcile trigger drops an unnecessary Sync bound. The crate contains no unsafe code or new external side effect."

[[audits.libc]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.2.189"
notes = "Reviewed complete 0.2.189 generated bindings and the 0.2.186 delta across supported targets; changed ptrace, NetBSD, VxWorks, and unstable-time bindings are not used by OxiBelt. The Linux tcp_info extension preserves the initialized prefix and older-kernel short-write contract used by OxiBelt's zeroed buffer."

[[audits.link-section]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.19.1"
notes = "Audited full source of link-section 0.19.1; linker and FFI unsafe operations are confined to documented section-layout invariants, safe section names are isolated by location hashing, untyped or custom-name submission requires explicit unsafe opt-in, and the 0.19.1 change only corrects Wasm macro expansion punctuation."

[[audits.link-section]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.19.1 -> 0.19.2"
notes = "Reviewed the complete checksum-matched 0.19.1-to-0.19.2 source delta; safe section names still include a location hash, but proc-macro definition-site spans under a validated non-root crate base contribute token content to avoid unrelated churn. Linker ranges, Wasm allocation, pointer, and FFI unsafe implementations are unchanged."

[[audits.link-section]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.19.2 -> 0.19.3"
notes = "Reviewed the complete checksum-matched 0.19.2-to-0.19.3 source delta; Wasm reference-section const cells now carry the same trailing metadata-slot layout as static cells, and materialization checks the null fix-up pointer before writing, preventing an out-of-bounds read and garbage-pointer write. Native linker and FFI paths and explicit unsafe opt-ins are unchanged, with no build script."

[[audits.linktime-proc-macro]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.0 -> 0.2.2"
notes = "Reviewed the complete checksum-matched 0.2.0-to-0.2.2 proc-macro delta. The optional ignore_base suffix is resolved only from compiler-provided Span::local_file, rejected for empty or filesystem-root bases, and changes hashing from location to token content only within that base; the macro does not read file contents, execute commands, use unsafe code, or access the network."

[[audits.linktime-proc-macro]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.2 -> 0.2.3"
notes = "Reviewed the complete checksum-matched 0.2.2-to-0.2.3 proc-macro delta; location hashes serialize line and column as fixed u64 chunks, alphabet reduction avoids host-sized truncation, and absolute spans are relativized before the existing filename fallback. It reads only compiler-provided spans and current_dir, contains no unsafe or build script, and performs no command, file-content, or network access."

[[audits.local-event]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.1.2 -> 0.1.3"
notes = "Reviewed the complete checksum-matched 0.1.2-to-0.1.3 source delta; resolving a notified listener now removes its entry and decrements the outstanding count so later Drop cannot transfer an already consumed notification. Production remains safe single-threaded Rc and RefCell code with no dependencies, build script, or external side effect."

[[audits.log]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.4.33 -> 0.4.34"
notes = "Reviewed the complete checksum-matched 0.4.33-to-0.4.34 package delta. It exposes the existing boxed-logger implementation through a new alloc feature and makes std imply alloc; OxiBelt still selects std, preserving its behavior. Existing logger atomics and unsafe code are unchanged, with no dependency, build script, IO, or other external capability added."

[[audits.num-integer]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.1.46 -> 0.1.47"
notes = "Reviewed the complete checksum-matched 0.1.46-to-0.1.47 source delta; the only runtime change implements normalized Karatsuba u128 square root with bounded intermediates and a checked correction step that preserves floor-square-root semantics. The crate adds no unsafe, build script, or external capability."

[[audits.object_store]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.14.1"
notes = "Reviewed the complete checksum-matched 0.14.1 source with OxiBelt's default-features-disabled aws and fs selection. The package has no build script; the selected S3 path builds signed requests, credentials, retry behavior, conditional writes, and endpoint URLs through its reqwest/rustls client, while local storage remains confined to the configured root. The crate-owned unsafe selected surface is limited to cfg-specific Windows local-file cleanup and is excluded from Linux release builds. OxiBelt's CT adapter further requires HTTPS in production, performs create-only and versioned conditional writes, constrains relative object keys, verifies readback, and redacts provider errors and credentials."

[[audits.online-dsl-forge]]
who = "OpenAI Codex <codex@openai.com>"
criteria = "safe-to-deploy"
delta = "0.2.0 -> 0.3.0"
notes = "Packaged production source and README are byte-identical to 0.2.0. The release only raises the MSRV and dependency floors already selected by OxiBelt and adds non-propagating development dependencies; it has no build script, proc macro, unsafe code, or new runtime capability."

[[audits.online-dsl-forge]]
who = "OpenAI Codex <codex@openai.com>"
criteria = "safe-to-deploy"
delta = "0.3.0 -> 0.3.1"
notes = "Reviewed the complete checksum-matched 0.3.0-to-0.3.1 source delta. The parser now tracks serialized AST depth and fails closed before serde_json's default container-depth boundary; tokenization, spans, AST construction, and existing recursion limits otherwise remain intact. The crate adds no unsafe code, build script, proc macro, filesystem, process, or network capability."

[[audits.online-dsl-forge]]
who = "OpenAI Codex <codex@openai.com>"
criteria = "safe-to-deploy"
delta = "0.3.1 -> 0.3.2"
notes = "Reviewed the complete checksum-matched 0.3.1-to-0.3.2 crate delta. Production source is unchanged; package metadata only enables serde_json's float_roundtrip feature, preserving finite floating-point literals across AST serialization without adding unsafe code, a build script, proc-macro behavior, or external capability."

[[audits.online-dsl-forge]]
who = "OpenAI Codex <codex@openai.com>"
criteria = "safe-to-deploy"
delta = "0.3.2 -> 0.3.3"
notes = "Reviewed the complete checksum-matched 0.3.2-to-0.3.3 source delta. Float literal tokenization now accepts only finite parsed values, routing overflow and non-finite results through the existing diagnostic path before AST construction or evaluation. Recursion and token limits are unchanged, and the crate contains no unsafe code, build script, proc macro, filesystem, network, or process capability."

[[audits.password-hash]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.5.0 -> 0.6.1"
notes = "Reviewed the complete checksum-matched 0.5.0-to-0.6.1 source delta. PHC parsing moved to the separately audited phc crate while hashing and verification traits became format-generic; verification still requires salt and output, propagates parsed parameters, and compares outputs through PHC constant-time equality. The crate forbids unsafe code, has no build script or external side effects, and OxiBelt selects only phc and alloc without RNG access."

[[audits.pest]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.8.8"
notes = "Reviewed the complete 2.8.8 parser and 2.8.7 diff; it has no unsafe, build script, or IO, and the source change only prevents node tagging during lookahead while retaining the opt-in parser call limit."

[[audits.pest]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.8.8 -> 2.9.0"
notes = "Reviewed the complete checksum-matched 2.8.8-to-2.9.0 parser delta; sequence parsing now snapshots and restores the grammar stack, and the new const Pratt API operates on Copy rule types and rejects chained operators. OxiBelt's transitive jsonpath grammar uses neither stack operations nor Pratt, and the crate adds no unsafe, build script, or external capability."

[[audits.pest_derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.8.8"
notes = "Reviewed the complete 2.8.8 proc-macro source and 2.8.7 diff; production code is unchanged and contains no unsafe, build script, or runtime side effects beyond delegated compile-time grammar generation."

[[audits.pest_derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.8.8 -> 2.9.0"
notes = "Reviewed the complete checksum-matched 2.8.8-to-2.9.0 proc-macro delta; production derive source is byte-for-byte unchanged apart from synchronized metadata and separately audited parser dependencies, with no unsafe, build script, or new external capability."

[[audits.pest_generator]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.8.8"
notes = "Reviewed the complete 2.8.8 generator and 2.8.7 diff; production code is unchanged, has no unsafe or build script, and its only external side effect is the documented compile-time read of a caller-selected grammar file."

[[audits.pest_generator]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.8.8 -> 2.9.0"
notes = "Reviewed the complete checksum-matched 2.8.8-to-2.9.0 generator delta; the production change is a semantics-equivalent match guard, and the existing caller-selected compile-time grammar read is unchanged. No unsafe, build script, command, or network capability is added."

[[audits.pest_meta]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.8.8"
notes = "Reviewed the complete 2.8.8 grammar parser and 2.8.7 diff; production code is unchanged, contains no unsafe, build script, or IO, and rejects left-recursive and non-progressing grammars."

[[audits.pest_meta]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.8.8 -> 2.9.0"
notes = "Reviewed the complete checksum-matched 2.8.8-to-2.9.0 grammar-parser delta; production source is byte-for-byte unchanged apart from synchronized dependency metadata. It remains safe Rust without a build script or external capability."

[[audits.phc]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.6.1"
notes = "Reviewed the complete checksum-matched 0.6.1 source. Parsing is allocation-bounded with fixed limits for identifiers, parameters, values, decoded salts, and outputs; Base64 is fully validated and output equality uses ctutils constant-time comparison. The crate forbids unsafe code and has no build script, I/O, networking, or process execution. OxiBelt selects alloc only; optional RNG capabilities are disabled."

[[audits.polyval]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.7.3"
notes = "Audited full source of polyval 0.7.3; architecture-specific unsafe intrinsics are gated by runtime CPU detection and operate on fixed-size in-bounds blocks, the portable fallback contains no side effects, and the new Zeroize implementation clears field storage without changing cryptographic behavior."

[[audits.portable-atomic]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.14.0"
notes = "Reviewed complete 1.14.0 source and the 1.13.1 delta across architecture backends; s390x, LSFE, interrupt, and atomic-128 fixes preserve alignment and ordering invariants. OxiBelt selects the fallback AtomicU64 path only through fastbloom for QUIC anti-replay state."

[[audits.portable-atomic]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.14.0 -> 1.15.0"
notes = "Reviewed the complete checksum-matched 1.14.0-to-1.15.0 source delta across architecture backends. Pre-v6 Arm uses documented kuser barriers for ordered small and pointer atomics, s390x 128-bit CAS retains alignment and SeqCst semantics while removing mixed-size speculative loads, and RISC-V feature detection remains target and toolchain gated. Build changes are local rustc cfg probes with no downloader or network access; OxiBelt's selected fallback path and release architectures do not use the newly changed pre-v6 Arm or s390x paths."

[[audits.proc-macro2]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.107"
notes = "Reviewed complete 1.0.107 source and the 1.0.106 delta; token ownership and literal fast-path unsafe contracts are contained, build-time execution is limited to Cargo-provided rustc probes in OUT_DIR, and the behavioral delta preserves parser and span-invalidation safety."

[[audits.quote]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.47"
notes = "Reviewed complete 1.0.47 source and the 1.0.46 delta; token generation has no external side effects, the build script only queries Cargo-provided rustc, literal fast paths operate on compiler-validated literals, and the source delta is documentation-only."

[[audits.rand]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.8.7 -> 0.8.8"
notes = "Reviewed the complete checksum-matched 0.8.7-to-0.8.8 production-source delta. The sole runtime edit uses core::char::MAX instead of std::char::MAX in the existing bounded UniformChar serde validation; random generation, unsafe code, dependencies, features, and build behavior are unchanged."

[[audits.ref-cast]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.26"
notes = "Audited full source of ref-cast 1.0.26; casts are restricted to representation-validated types, unsafe conversions preserve reference layout, and build.rs only queries the configured Rust compiler version and writes generated configuration into OUT_DIR, with no network or uncontrolled filesystem side effects."

[[audits.ref-cast-impl]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.26"
notes = "Audited full source of ref-cast-impl 1.0.26; the proc macro rejects unsupported representations and nontrivial field layouts before generating confined pointer casts, and performs no filesystem, network, process, or environment side effects."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.48.5"
notes = "Reviewed the complete 0.48.5 source and confirmed it is production-identical to 0.48.1; default retrieval performs no IO, and registry crawling uses deduplication plus a depth-128 iterative fallback."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.48.5 -> 0.49.2"
notes = "Reviewed the complete checksum-matched 0.48.5-to-0.49.2 source delta; it only adds a const getter mapping Draft 4 to id and later or unknown drafts to $id, with retrieval, registry traversal, recursion limits, unsafe boundaries, and external side effects unchanged."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.2 -> 0.49.4"
notes = "Reviewed the complete checksum-matched 0.49.2-to-0.49.4 crate delta; resolver, URI, retrieval, dynamic-scope, and recursive-reference production source is byte-for-byte unchanged and only synchronized package metadata moved. The selected graph adds no unsafe code, build script, filesystem, network, or process capability."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.4 -> 0.49.8"
notes = "Reviewed the complete checksum-matched 0.49.4-to-0.49.8 source delta. Draft detection now maps the standard version-less json-schema.org URI to Draft 2020-12; resolver, retrieval, registry traversal, recursion controls, and external capabilities are unchanged. The crate contains no unsafe code or build script."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.49.8 -> 0.49.9"
notes = "Reviewed the complete checksum-matched 0.49.8-to-0.49.9 crate delta; resolver, retrieval, registry, URI, and recursion-control production source is byte-for-byte unchanged and only synchronized package metadata moved. The crate adds no unsafe, build script, filesystem, network, or process capability."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.50.0 -> 0.51.0"
notes = "Reviewed the complete checksum-matched 0.50.0-to-0.51.0 crate delta and signed upstream release commits. Version 0.50.1 is source-identical to 0.50.0; 0.51.0 adds the embedded Draft 2020-12 Format-Assertion meta-schema, distinguishes its vocabulary, reads required vocabularies even without an id, and injects only a fixed set of bundled schemas. Resolution, retrieval, URI handling, and traversal controls add no unsafe code, build script, filesystem, network, process, or other external capability."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.51.0 -> 0.52.0"
notes = "Reviewed the complete checksum-matched 0.51.0-to-0.52.0 crate delta and signed upstream release commit. The sole production change makes an explicitly forced draft supply standard vocabularies while preserving custom meta-schema vocabulary lookup. Resolver, URI, retrieval, registry deduplication, and the depth-128 iterative traversal fallback are unchanged; the default retriever performs no I/O, and OxiBelt selects neither async retrieval nor any external fetch capability. The crate contains no unsafe code or build script."

[[audits.referencing]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.52.0 -> 0.52.1"
notes = "Reviewed the complete checksum-matched 0.52.0-to-0.52.1 crate delta; resolver, retrieval, registry, URI, and recursion-control production source is byte-for-byte unchanged and only synchronized package metadata moved. The crate adds no unsafe code, build script, filesystem, network, or process capability."

[[audits.regex-automata]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.4.16 -> 0.4.18"
notes = "Reviewed the complete checksum-matched 0.4.16-to-0.4.18 source delta; meta-regex cache pools gain an explicit capacity with a positive minimum and an available-parallelism default, and cloned regexes preserve that setting. Existing UnsafeCell ownership, thread-ID exclusivity, Send and Sync invariants, search engines, and input bounds are unchanged, and the crate adds no build script or external side effects."

[[audits.rustls]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.23.42 -> 0.23.43"
notes = "Reviewed the complete checksum-matched 0.23.42-to-0.23.43 source delta. Client and server negotiation now restrict versions and cipher suites to the active TCP or QUIC protocol and the exact offered suite set; truncated tickets, binders, and ticket ages use checked or saturating arithmetic. The crate continues to deny unsafe code, and key-log filesystem access remains explicit and unchanged."

[[audits.rustls-pki-types]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.15.1"
notes = "Audited full source of rustls-pki-types 1.15.1; certificate and key wrappers, bounded PEM and base64 decoding, IP parsing, and the revised DNS validator preserve input bounds and secret-handling boundaries; the sole fixed-array byte reinterpretation is size and alignment safe, with no build script, FFI, networking, or command execution."

[[audits.rustls-webpki]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.103.13 -> 0.103.14"
notes = "Reviewed the complete checksum-matched 0.103.13-to-0.103.14 source delta; ML-DSA exports are stabilized for aws-lc-rs and the CRL iteration refactor is equivalent. OxiBelt does not use ALL_VERIFICATION_ALGS: rustls keeps its curated certificate list and OxiBelt OCSP keeps an explicit algorithm list, so trust policy is not widened. The delta adds no unsafe or build script."

[[audits.rustls-webpki]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.103.14 -> 0.103.15"
notes = "Reviewed the complete checksum-matched 0.103.14-to-0.103.15 source delta; signature verification is unchanged, while AWS-LC algorithms now derive FIPS status solely from runtime FIPS mode so ML-DSA is reported as submitted or pending with AWS-LC-FIPS 4.x. OxiBelt uses the non-FIPS aws-lc-rs build, where the status remains false, and its curated TLS and OCSP algorithm sets are unchanged. The delta adds no unsafe code, build script, FFI, I/O, networking, or command execution; feature-matched archive build and OxiBelt TLS-root tests pass."

[[audits.serde]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.229"
notes = "Reviewed the complete 1.0.229 source and 1.0.228 diff; both UTF-8 unsafe sites preserve their invariants, hinted collection preallocation is capped at 1 MiB, and the build script only writes OUT_DIR and queries RUSTC --version."

[[audits.serde-saphyr]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.0.29 -> 1.0.1"
notes = "Reviewed the complete checksum-matched production-source delta and OxiBelt's default-features-disabled deserialize graph. The crate forbids unsafe code, has no build script, and selected features cannot access files, network, processes, or environment state. Deserialization budgets bound events, documents, nodes, depth, scalar and comment bytes, anchors, aliases, merge keys, replay events, and replay depth; breaches latch, alias ratios finalize before reset, recursive and unknown aliases reject, reader errors propagate, duplicate mappings reject by default, and NUL, invalid controls, and typeless non-finite floats fail closed."

[[audits.serde-saphyr]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.1.0 -> 1.2.0"
notes = "Reviewed the complete checksum-matched 1.1.0-to-1.2.0 production-source delta and OxiBelt's default-features-disabled deserialize graph. The crate continues to forbid unsafe code and has no build script. Deserialization adds budgets for recorded-anchor events and bytes, property interpolation work, and block and directive nesting, while explicit tag node-kind checks reject mismatches. Optional include and command-line filesystem paths plus serialization remain unselected; existing compatibility defaults are preserved and all selected limit breaches fail closed."

[[audits.serde_core]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.229"
notes = "Reviewed the complete 1.0.229 core and 1.0.228 diff; both UTF-8 unsafe sites preserve their invariants, hinted collection preallocation is capped at 1 MiB, and the build script only writes OUT_DIR and queries RUSTC --version."

[[audits.serde_derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.229"
notes = "Reviewed the complete 1.0.229 proc macro and 1.0.228 diff; it has no unsafe, build script, or external IO, and the source changes are a Syn 3 API migration with equivalent generated serialization and deserialization structure."

[[audits.serde_json]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.151"
notes = "Reviewed the complete 1.0.151 source and 1.0.150 diff; all unsafe sites preserve documented bounds, UTF-8, and representation invariants, the parser retains its depth-128 limit, and the new contract-checked RawValue unsafe API is not enabled in this graph."

[[audits.strum]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.28.0"
notes = "Reviewed complete new 0.28.0 source; it contains safe enum traits and supporting data types with no unsafe code, build script, or filesystem, environment, network, or process side effects."

[[audits.strum_macros]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.28.0"
notes = "Reviewed complete new 0.28.0 proc-macro source; syntax parsing and generated implementations use safe code, have no file, network, or process effects, and the only environment behavior is explicitly enabled STRUM_DEBUG diagnostic output."

[[audits.syn]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "3.0.3"
notes = "Reviewed complete new 3.0.3 source; token-buffer cursor, parser-lifetime, thread-bound, and token-layout unsafe invariants are documented and maintained, parsing failures return structured errors, and the crate has no build script or runtime external side effects."

[[audits.syn]]
who = "OpenAI Codex <codex@openai.com>"
criteria = "safe-to-deploy"
delta = "3.0.3 -> 3.0.4"
notes = "Reviewed the complete checksum-matched 3.0.3-to-3.0.4 source delta. The only parser behavior change routes foreign-function signatures through the existing signature parser with safe-function syntax enabled; unsafe implementation and token-buffer code are unchanged, and the crate adds no build script or external side effect."

[[audits.synchrony]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.1.9"
notes = "Reviewed complete 0.1.9 source and the 0.1.8 delta; the synchronization unsafe implementations and interior-mutability code are byte-for-byte unchanged, the sole source change publicly reexports the standard Arc and Rc Weak types, and the crate retains its MIT license and has no build script."

[[audits.thiserror]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.0.19"
notes = "Reviewed complete 2.0.19 source and the 2.0.18 delta; runtime behavior remains a derive facade, build-time writes and compiler probes are confined to OUT_DIR using Cargo-provided rustc, and the functional delta is an equivalent Option predicate update."

[[audits.thiserror]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.0.19 -> 2.0.20"
notes = "Reviewed the complete checksum-matched 2.0.19-to-2.0.20 source delta. Runtime behavior remains a safe derive facade; the private-version marker advances with the separately audited proc macro, and other production changes are documentation-only. Existing compiler probes and OUT_DIR writes are unchanged."

[[audits.thiserror-impl]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.0.19"
notes = "Reviewed complete 2.0.19 proc-macro source and the 2.0.18 delta; derive parsing, validation, and code generation contain no unsafe or external side effects, and the only functional changes are equivalent Option predicate rewrites."

[[audits.thiserror-impl]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.0.19 -> 2.0.20"
notes = "Reviewed the complete checksum-matched 2.0.19-to-2.0.20 proc-macro delta. Generated initializers add only targeted lint allowances for explicit field names; derive parsing, validation, output semantics, unsafe boundaries, and external side effects are unchanged."

[[audits.tokio]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.53.1"
notes = "Reviewed complete 1.53.1 source and the 1.52.4 delta; mpsc wake and drop handling, zero-length Chain reads, timer resets, and FastRand state fixes preserve runtime, task, buffer, and I/O-driver invariants. The crate adds no build script and retains its MSRV and license."

[[audits.tokio-macros]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "2.7.1"
notes = "Reviewed complete 2.7.1 proc-macro source and the 2.7.0 delta; the production macro implementation is unchanged, contains no unsafe or external side effects, and the release delta is documentation-only."

[[audits.tokio-macros]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.7.1 -> 2.7.2"
notes = "Reviewed the complete checksum-matched 2.7.1-to-2.7.2 proc-macro delta; nested impl Trait detection now follows Syn 3 parenthesized argument nodes through their type field, preserving generated runtime and test wrappers, with no unsafe code, build script, filesystem, environment, network, or process access."

[[audits.tokio-stream]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.1.19"
notes = "Reviewed complete 0.1.19 source and the 0.1.18 delta; StreamMap overflow and limit handling now fail safely while stream pinning, wakeup, and removal behavior remain bounded, with no build script or external side effects."

[[audits.tokio-util]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.7.19"
notes = "Reviewed complete 0.7.19 source and the 0.7.18 delta; queue, codec, cancellation, and I/O fixes preserve buffer and wakeup invariants. OxiBelt's selected io feature compiles the new vectored-write helper; its checked slice advancement and lifetime restoration preserve the underlying IoSlice bounds, and OxiBelt has no call site for it."

[[audits.toml]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.1.3+spec-1.1.0 -> 1.1.4+spec-1.1.0"
notes = "Reviewed the complete checksum-matched 1.1.3-to-1.1.4 source delta; owned Table and Value deserializers explicitly route datetime structs through the existing typed DatetimeDeserializer and all other structs through deserialize_any. The crate remains safe Rust with no build script or external side effects."

[[audits.toml_parser]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "1.1.2+spec-1.1.0 -> 1.1.3+spec-1.1.0"
notes = "Reviewed the complete checksum-matched 1.1.2-to-1.1.3 source delta; token-vector preallocation now uses one slot per four input bytes instead of an input-length allocation, reducing attacker-controlled over-allocation. Parser bounds and the opt-in unsafe feature's checked UTF-8 and span invariants are unchanged, with no build or external IO side effects."

[[audits.wasm-bindgen]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.126 -> 0.2.127"
notes = "Reviewed the complete checksum-matched 0.2.126-to-0.2.127 source delta. JsNullable models null-or-undefined through existing checked JsValue conversions and type-level upcasts, while the Emscripten marker remains a one-byte static moved behind its target cfg. ABI conversions preserve ownership and no build script, filesystem, network, or process effect is added."

[[audits.wasm-bindgen-macro]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.126 -> 0.2.127"
notes = "Reviewed the complete checksum-matched 0.2.126-to-0.2.127 crate delta. Proc-macro source is byte-for-byte unchanged and package metadata only advances the separately audited macro-support dependency; the crate contains no unsafe code, build script, or external side effect."

[[audits.wasm-bindgen-macro-support]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.126 -> 0.2.127"
notes = "Reviewed the complete checksum-matched 0.2.126-to-0.2.127 proc-macro support delta. Generated ABI descriptors count Unicode scalars correctly, namespace hashes avoid import collisions, slice-to-array rejects mutable and generic shapes that cannot preserve writes or ABI bounds, and core/alloc paths remain hygienic. Existing transmute code keeps ManuallyDrop ownership and concrete ABI-shape constraints; the macro performs no filesystem, network, or process IO."

[[audits.wasm-bindgen-shared]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.2.126 -> 0.2.127"
notes = "Reviewed the complete checksum-matched 0.2.126-to-0.2.127 crate delta. Production shared-schema source is byte-for-byte unchanged and only synchronized package metadata moved; the crate contains no unsafe code, build script, or external side effect."

[[audits.web-transport-quinn]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.11.10 -> 0.11.12"
notes = "Reviewed the complete checksum-matched 0.11.10-to-0.11.12 source delta. SendStream now retains caller-buffer position across cancellation by using the trait's partial-write default, owned zero-copy chunks remain explicit, and client/server builders share and actually attach the selected bounded Quinn transport configuration. The crate has no unsafe code; the separate web-transport-trait 0.4.0 exception remains exact."

[[audits.web-transport-quinn]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.11.12 -> 0.12.0"
notes = "Reviewed the complete checksum-matched 0.11.12-to-0.12.0 source delta. Private operation retention and kio-backed waiter and fan handling keep notifier registrations through Pending, discard abandoned weak registrations, start cloned handles idle, and deliver deferred wakeups after releasing the shared accept lock. Async and poll receive implementations delegate to Quinn's initialized byte-slice APIs and the crate contains no unsafe code or build script. OxiBelt disables default features and selects aws-lc-rs, excluding ring and qlog; web-transport-trait remains separately exempted because its generic receive defaults are unsafe."

[[audits.webpki-root-certs]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.9"
notes = "Reviewed the complete generated 1.0.9 trust-anchor set and verified release provenance; four Mozilla-approved Telia and SECOM roots are added and the removed Atos TrustedRoot 2011 anchor is dropped, changing the set from 118 to 121 anchors with no executable code or build script."

[[audits.webpki-roots]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "1.0.9"
notes = "Reviewed complete 1.0.9 facade source and verified release provenance and the synchronized webpki-root-certs 1.0.9 trust delta; the crate contains no unsafe code, build script, parsing, network access, or other side effects."

[[audits.whoami]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "2.1.2 -> 2.1.3"
notes = "Reviewed the complete checksum-matched 2.1.2-to-2.1.3 source delta; the native runtime change adds MATE desktop detection and internal Cow cleanup, while WASI and Redox changes are irrelevant to OxiBelt's Linux graph. OxiBelt receives the crate only through sqlx-postgres, and no unsafe, build script, or external capability is added."

[[audits.xxhash-rust]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.8.18"
notes = "Audited full source of xxhash-rust 0.8.18; unsafe loads are internal and preceded by slice-length checks, the XXH3 builder now applies seed and secret precedence consistently, and OxiBelt uses it only for non-cryptographic structural hashing through Sequoia, with no build-time or runtime side effects."

[[audits.zerocopy]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.8.55"
notes = "Reviewed complete 0.8.55 source and the 0.8.54 delta; runtime changes are documentation, Order derives, and a hidden identity helper, while representation, alignment, initialization, and SIMD conversion invariants remain unchanged."

[[audits.zerocopy]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.8.55 -> 0.8.56"
notes = "Reviewed the complete checksum-matched 0.8.55-to-0.8.56 source delta. Runtime representation, alignment, initialization, pointer, and SIMD logic is unchanged; the only production edit removes a verification-only cfg from an alloc import and advances the separately audited derive dependency. No build script or external side effect is added."

[[audits.zerocopy-derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
version = "0.8.55"
notes = "Reviewed complete 0.8.55 proc-macro source and the 0.8.54 delta; homogeneous generic repr(C) IntoBytes support is emitted only with rustc-enforced type-identity predicates that preserve layout and macro-hygiene soundness, with no external side effects."

[[audits.zerocopy-derive]]
who = "OpenAI Codex"
criteria = "safe-to-deploy"
delta = "0.8.55 -> 0.8.56"
notes = "Reviewed the complete checksum-matched 0.8.55-to-0.8.56 proc-macro delta. The crate attribute now accepts validated absolute, crate-relative, self-relative, and super-relative module paths, rejects malformed keyword placement, and absolutizes only external paths; generated layout and conversion proofs are otherwise unchanged. The macro contains no unsafe code or external side effects."
